#ifndef CORES_H
#define CORES_H

// Declaração da função para mudar a cor do texto no console.
// O corpo desta função (usando SetConsoleTextAttribute no Windows
// ou sequências ANSI no Linux) deve estar em cores.c.
void mudarCor(int cor);

#endif